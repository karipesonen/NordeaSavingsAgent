# Demo

The user-facing prototype — whatever form it takes (web app, Figma + scripted agent, Streamlit, etc.).

Current demo shape (from `../notes/concept.md`):
- Financial Cockpit (current state at a glance)
- Smart Recommendation Card (one agent-generated card at a time)
- Trust Ledger panel (why the agent recommended it)

Decision pending: full interactive build vs. scripted demo-only prototype. Resolve before writing code.
